Can pop up a second window possibly?
If possible, have the serial output go to the code that handles both windows.
One window can handle the L-COM directly, and the second window can be application specific, e.g. the connected device commands (that are sent through message packets).